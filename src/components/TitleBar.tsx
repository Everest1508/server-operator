export function TitleBar() {
  return (
    <div className="h-8 shrink-0 bg-[var(--bg-secondary)] border-b border-[var(--border)] flex items-center justify-center text-xs text-[var(--text-secondary)] select-none">
      Server Operator
    </div>
  );
}
